import EnvironmentPageWrapper from "@/components/ui/environment/EnvironmentPageWrapper";

export default function DatasetPage() {
  return (
    <EnvironmentPageWrapper>
      <h1 className="text-2xl font-bold">Dataset</h1>
      <p className="text-muted-foreground mt-1">Upload and manage your training data.</p>
      <div className="mt-8 text-muted-foreground text-sm">🚧 Coming soon — let's build this!</div>
    </EnvironmentPageWrapper>
  );
}